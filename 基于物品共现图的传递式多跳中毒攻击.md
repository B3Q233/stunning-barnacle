# 传递式路径投毒攻击（Transitive Path Attack, TPA）研究方案

> 面向推荐系统安全的多跳语义/共现路径投毒框架

------

## 摘要

现有推荐系统投毒攻击（如 IndirectAD）通过单一"触发商品"实现目标商品的间接引流，但其"基座→触发物→目标物"的两跳结构在跳变幅度上仍然较大，存在被行为异常检测捕获的风险。本文提出 **传递式路径投毒攻击（TPA）**，将两跳桥接扩展为**可变长度的多跳传递路径**，通过"平庸用户基座 + 语义/共现融合路径 + 目标商品"的三段式框架，使伪造用户的兴趣演化轨迹在统计上更接近真实用户的渐进式兴趣漂移，从而在同等甚至更低的投毒比例下实现更强的攻击效果与更高的隐蔽性。

------

## 1. 问题背景

### 1.1 传统投毒攻击的局限

```
传统假用户画像:  [随机物品] [随机物品] [目标物品 IT]
                        ↑
                   行为模式突兀，容易被异常检测捕获
```

- 假用户交互序列缺乏**行为合理性**，从普通物品到目标物品的跳转过于生硬
- 基于流行度分布、图结构异常的检测方法容易识别此类"硬连接"

### 1.2 IndirectAD 的改进与残余问题

IndirectAD（arXiv:2511.05845, 2025）引入"触发商品"作为中间载体：

```
攻击路径:  目标用户 → 触发商品 → 目标商品
策略: 选择最容易被推广的商品作为触发器，构造假用户同时与触发器和目标商品交互
```

该方案将投毒比例下限从 1% 压低到 0.05%，但其结构仍是**固定两跳**、**单一桥接物品**，跳变幅度（如"运动发带→政治帽子"）在语义空间上依然明显，一旦防御方引入语义一致性检测，容易被识别为异常共现对。

### 1.3 核心洞察

真实用户的兴趣转移是**渐进式**的，在物品共现图 / 语义空间上表现为一条**自然路径**，而非一次性跳跃。将"一次大跳变"拆解为"多次小跳变"，理论上能同时提升攻击的隐蔽性与可迁移性。

```
本文方案:  I1 → I2 → I3 → (自然过渡) → I4 → I5 → I6 → IT
            └── 平庸基础 ──┘   └── 传递路径 ──┘    └─ 目标 ─┘
```

------

## 2. 方法框架

### 2.1 整体流程

```
Step 1: 构建"平庸用户"基座
Step 2: 构造传递路径（基座 → 目标商品）
Step 3: 复合损失优化 + 注入
```

### 2.2 Step 1：平庸用户基座构建

基于全局数据统计，而非借鉴/复制单个真实用户，合成一个统计意义上的"大众化"基础画像：

- 计算物品流行度分布 $P(i)$
- 计算用户语义兴趣中心（若引入 CLIP-like 编码器 $\phi(\cdot)$）： $$c = \frac{1}{|U|}\sum_{u \in U} \bar{e}_u, \quad \bar{e}_u = \text{mean}({e_i : i \in \text{interacted}(u)}),\quad e_i = \phi(i)$$
- 平庸基座 ${I_{\text{base}}}$：从 $P(i)$ 中采样 $K$ 个高流行度、且语义上接近 $c$ 的物品

> 与 CopyAttack / R-Trojan 的区别：后者复制/裁剪**单个真实用户模板**，本方案是**全局统计合成**，不依赖任何单一真实个体。

### 2.3 Step 2：传递路径构造

目标：从基座 $I_{\text{base}}$ 到目标商品 $I_T$ 构造一条自然过渡路径，三种候选策略：

**(a) 最短路径法** 在物品图上求 $I_{\text{base}} \to I_T$ 的最短路径，边权可用融合距离： $$d'(i,j) = \lambda \cdot d_{\text{sem}}(i,j) + (1-\lambda) \cdot d_{\text{CF}}(i,j)$$ 其中 $d_{\text{sem}}$ 为语义距离（$1-\cos(e_i,e_j)$），$d_{\text{CF}}$ 为替代模型物品隐向量距离，避免单纯语义相近但统计上从不共现（或反之，"啤酒尿布"式强共现但语义无关）的问题。每跳距离约束：$\max_k d'(I_k,I_{k+1}) \le \tau$

**(b) 随机游走法** 从 $I_{\text{base}}$ 出发做 biased random walk，每步以概率 $p$ 继续游走、以概率 $1-p$ 收敛到 $I_T$，模拟真实用户浏览的随机性与非线性

**(c) 锚点桥接法** 寻找一组与基座和 $I_T$ 均有共现关系的"桥接物品"集合，构造： $$\text{假用户} = I_{\text{base}} + {I_{\text{bridge}}} + I_T$$

### 2.4 Step 3：复合损失优化与注入

沿用对抗优化范式，将 IndirectAD 的两项复合损失推广为多跳加权形式： $$\mathcal{L}*{composite} = \sum*{k=1}^{n} \beta_k \cdot \mathcal{L}_{I_k}$$

- $\beta_k$ 可设计为随链条推进递增/递减，控制优化"火力"分布
- 引入时间衰减权重，让越接近 $I_T$ 的路径物品交互权重越高，模拟"逐渐发现新兴趣"的行为
- 采用 PGD 迭代更新伪造交互数据，注入约束保证路径上每一跳共现关系自然形成

------

## 3. 关键挑战

### 3.1 路径长度的 trade-off

| 路径类型 | 隐蔽性 | 攻击信号强度     |
| -------- | ------ | ---------------- |
| 短路径   | 差     | 强               |
| 长路径   | 好     | 弱（信号被稀释） |

路径越长，$I_T$ 在假用户画像中的相对权重越低，优化信号越弱，需要在 $\beta_k$ 设计和路径长度之间寻找平衡点。

### 3.2 物品图稀疏性

长尾场景下物品对之间可能不存在直接共现，导致路径缺失或需要跨过大量跳数。解决思路：引入内容特征（文本/图像 embedding）补全稀疏区域，或使用图补全技术。

### 3.3 防御方的针对性反制

若防御方察觉路径式攻击的存在，可能：

- 检测物品共现图上的异常密集路径
- 监控物品"桥接度"（betweenness centrality）的异常上升
- 构建语义连续性检测器，识别"过于工整"的兴趣演化轨迹（真实用户的兴趣漂移通常带噪声、有回退）

这构成攻防双方的 min-max 博弈，建议在路径构造中主动加入随机扰动/回退，避免轨迹过于平滑而暴露。

------

## 4. 文献定位与新颖性分析

### 4.1 新颖性评估

| 维度               | 新颖程度 | 说明                                             |
| ------------------ | -------- | ------------------------------------------------ |
| 平庸用户基座       | 较新     | 全局统计合成 + 路径起点，文献中未见直接对应      |
| 多跳传递路径       | 部分新   | 两跳桥接已有（IndirectAD）；≥3跳显式路径构造未见 |
| 路径构造策略对比   | 较新     | 最短路径/随机游走/锚点桥接的系统对比未见         |
| 路径长度 trade-off | 新       | 该消融维度未见系统研究                           |
| 整体框架           | 部分新   | "基座→路径→目标"三段式组合具有一定新颖性         |

### 4.2 相关工作对比

**最接近工作：IndirectAD（arXiv:2511.05845, 2025）**
     
| 维度         | IndirectAD                 | 本方案                                    |
| ------------ | -------------------------- | ----------------------------------------- |
| 跳数         | 固定两跳                   | 可变多跳                                  |
| 桥接物品数量 | 1个                        | 多个                                      |
| 起点选择     | 优化选择最容易推广的触发物 | 全局统计构建平庸基座                      |
| 路径构造     | 对抗优化（PGD）            | 图路径构造（最短路径/随机游走）+ 对抗优化 |

**其他相关工作**

| 论文                                      | 核心做法                                   | 与本方案差异                               |
| ----------------------------------------- | ------------------------------------------ | ------------------------------------------ |
| Average Attack（经典启发式）              | 填充物用全局平均分                         | 仅评分取均值，无用户画像结构概念           |
| CopyAttack（TKDE 2023）                   | 复制真实用户模板                           | 用单一真实个体，非全局合成                 |
| R-Trojan（ICDM 2023）                     | 采样真实用户模板并裁剪                     | 依赖单个真实用户，不涉及"平庸化"           |
| Leg-UP / RecUP（GAN-based）               | 从真实分布学习生成                         | 黑盒生成，无显式"基座+路径"结构            |
| Fake Co-Visitation Injection（NDSS 2017） | 直接注入伪造共现计数，操纵图结构边权重     | 本方案注入的是用户画像，经训练间接影响模型 |
| PC-Attack（AAAI 2023）                    | 2-hop 共现用于跨系统迁移攻击的对比学习视角 | 目标是可迁移图表示，非路径构造             |
| CREAT（arXiv:2511.09392, 2025）           | 最优传输约束的序列推荐隐蔽攻击             | 用分布匹配，非显式图路径                   |

> **风险提示**：IndirectAD 已提出"桥接物品传递攻击信号"的核心直觉，若本方案仅停留在"两跳→多跳"的数量扩展，增量贡献不足以支撑一篇独立论文，必须在路径构造理论、隐蔽性论证、防御反制等维度做出实质性区分（见第5节）。

------

## 5. 差异化策略

### 5.1 论证多跳路径的行为合理性

不能止步于"多跳更隐蔽"的直觉断言，需要：

- 在真实数据集上统计分析用户兴趣漂移的路径长度分布、跳数分布
- 实证对比：真实用户 vs. 传统假用户 vs. TPA假用户 的路径统计特征分布，证明后者更接近真实分布

### 5.2 显式建模兴趣演化

引入时序权重与行为噪声，使交互序列呈现自然的渐进漂移轨迹，而非机械的等权重路径。

### 5.3 与 IndirectAD 的直接对比

- 复现 IndirectAD 作为核心基线
- 证明 ≥3 跳路径在隐蔽性指标上具有统计显著提升
- 分析"何时多跳显著优于两跳桥接"的边界条件（如目标商品与主流兴趣的语义/共现距离越大，多跳优势越明显）

### 5.4 防御视角的正面论证

针对基于"兴趣突变检测"的防御方法，论证路径攻击天然免疫：攻击信号沿路径逐渐衰减，不产生突变特征，假用户序列在统计上更难与真实用户区分。同时也应诚实讨论：若防御方专门构建语义连续性检测器，路径式攻击的"过于工整"反而可能成为新的可疑信号，并据此提出初步缓解方案（加入随机扰动/回退）。

------

## 6. 实验设计

### 6.1 数据集

- MovieLens-1M / MovieLens-20M（经典基准）
- Amazon Books / Amazon Beauty（电商场景，含文本描述）
- Steam（游戏，含标签元数据）
- 至少一个含图像的数据集，如 Amazon Clothing（体现多模态语义编码器价值）
- 建议加入一个序列推荐数据集，如 Yoochoose / Taobao

### 6.2 受害模型

- 传统 CF：WRMF、BPR
- Autoencoder 类：Mult-VAE、ItemAE
- GNN 类：LightGCN、NGCF
- 序列模型：SASRec（验证路径式攻击对"渐进兴趣建模"模型的独特影响）

### 6.3 对比基线

- Random / Average / Segment / Bandwagon 等经典托攻击
- GAN-based：Leg-UP
- 跨域模板：CopyAttack
- **IndirectAD（核心基线，两跳桥接）**
- Transitive Path Attack（本方案，含 a/b/c 三种路径策略变体）

### 6.4 评估维度与指标

| 维度           | 指标                                                         |
| -------------- | ------------------------------------------------------------ |
| 攻击有效性     | HR@10/20/50、NDCG@20，不同投毒比例 0.01%,0.05%,0.1%          |
| 隐蔽性（检测） | 传统统计检测（Catch the Black Sheep）、深度检测（GraphRfi 等）的 AUC / F1 |
| 隐蔽性（分布） | 假用户画像与真实用户的 KL 散度 / MMD 距离                    |
| 鲁棒性/迁移性  | 替代模型生成的攻击数据迁移至其他受害模型的效果衰减           |
| 路径长度消融   | $n\in{1,2,3,5,8}$ 下 HR@20 与检测 AUC 的双轴曲线             |
| 路径策略对比   | 最短路径 / 随机游走 / 锚点桥接 的效果与隐蔽性对比            |
| 距离度量消融   | 纯语义距离 / 纯CF距离 / 融合距离（扫描 $\lambda$）对比       |
| 成本-收益      | 总伪造交互量（而非仅账号数）vs. HR@20 增幅                   |
| 目标商品属性   | 热度分层（头部/中上/中下/长尾）+ 语义孤立度分层              |
| 目标用户属性   | 聚类用户 vs. 随机用户；用户兴趣与目标商品语义匹配度分层      |

### 6.5 消融矩阵示例

| 变体                   | 语义路径规划 | 融合CF距离 | 链式共现 | HR@20 | 检测AUC |
| ---------------------- | ------------ | ---------- | -------- | ----- | ------- |
| IndirectAD（原始基线） | ✗            | ✗          | ✗        | -     | -       |
| +语义单触发            | ✓            | ✗          | ✗        | -     | -       |
| +语义链（无融合）      | ✓            | ✗          | ✓        | -     | -       |
| TPA 完整方法           | ✓            | ✓          | ✓        | -     | -       |

### 6.6 案例分析

挑选代表性案例，用 t-SNE 将 $I_{\text{base}} \to I_1 \to \cdots \to I_T$ 的语义路径可视化，直观展示"渐进过渡"相对于 IndirectAD"单点跳变"的轨迹差异。

------

## 7. 关键参考文献

| 论文                                             | 发表                    | 与本方案关系                                         |
| ------------------------------------------------ | ----------------------- | ---------------------------------------------------- |
| IndirectAD                                       | arXiv:2511.05845 (2025) | 最接近，两跳桥接攻击，核心对比基线                   |
| Fake Co-Visitation Injection Attacks             | NDSS 2017               | 物品共现图攻击，概念相关但机制不同（直接操纵图结构） |
| PC-Attack                                        | AAAI 2023               | 共现模式用于跨系统攻击迁移                           |
| CopyAttack+                                      | IEEE TKDE 2023          | 跨域真实用户复制，模板攻击方向                       |
| Data Poisoning Attacks to Deep Learning Based RS | NDSS 2021               | 深度推荐系统攻击奠基性工作                           |
| R-Trojan                                         | ICDM 2023               | 融合文本评论的逼真假用户生成                         |
| CREAT                                            | arXiv:2511.09392 (2025) | 序列推荐的分布约束隐蔽攻击                           |
| Leg-UP                                           | IEEE TNNLS 2022         | GAN 生成假用户画像的经典方法                         |
| DDSP                                             | SIGIR 2025              | 序列推荐的可感知多样性攻击                           |
| Poisoning Attacks and Defenses in RS: A Survey   | CSCWD 2025              | 投毒攻击综述，框架参考                               |

------

## 8. 投稿建议

若完成以下工作，可支撑一篇完整的会议/期刊论文：

1. 形式化定义 path-constrained profile construction
2. 系统性提出并对比多种路径构造策略
3. 充分的对比实验，尤其需包含 IndirectAD 直接对比
4. 路径长度消融实验 + 隐蔽性/检测抗性分析
5. 论证多跳相对两跳桥接的统计显著优势
6. 提出并初步验证至少一种针对性防御思路（体现研究的双向价值）

**目标venue参考：**

- 会议：CIKM（CCF-B）、WSDM（CCF-B）、ECML-PKDD（CCF-B）、RecSys、SIGIR
- 期刊：TOIS（CCF-A）、TKDE（CCF-A）、Information Sciences（JCR Q1）

------

## 9. 核心贡献点总结

> 在推荐系统投毒攻击中首次引入"用户行为合理性"的显式约束，通过物品共现图与语义空间融合的多跳传递路径，增强攻击的隐蔽性与可迁移性。提出"平庸基座 + 传递路径 + 目标商品"的三段式假用户构造框架，系统性研究路径长度、路径构造策略与攻击效果/隐蔽性之间的 trade-off，并同步探讨针对该类攻击的检测与防御思路，为攻防两端提供分析基础。

------

## 附：Ethics Statement 建议（投稿时需附加）

本研究旨在揭示现有推荐系统在投毒攻击面前的脆弱性，所有实验均在公开数据集与本地部署的开源推荐模型上完成，未对任何真实在线平台发起攻击。研究团队承诺在论文发表前通过负责任的披露渠道（如平台安全团队、CVE等）告知相关技术风险，并在第7节讨论了初步的防御缓解方案，希望以攻促防、推动推荐系统鲁棒性研究的发展。
