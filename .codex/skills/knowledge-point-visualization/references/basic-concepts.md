# 基础概念覆盖清单（Basic Concept Coverage）

目的：生成任何“讲解/推导型 HTML”（如论文推导可视化页）之前，逐项做覆盖检查。
凡在公式或正文中出现过的概念，必须在页面中找到高中水平的最小解释，禁止“默认读者会”。

## 检查规则

1. 把缺失的概念集中写进一个“基础工具箱”小节（例如 `1.0 基础工具箱`）。
2. 每个概念给出：直观定义 + 最小公式（若需要）+ 在论文哪里用到。
3. 后续章节用到时只引用、不重复解释。
4. 概念若在输出中出现但清单中没有，说明清单需要扩充——扩充后回写本文件。

## 强制清单

| 概念 | 出现位置示例 | 最小解释（必须包含） |
|---|---|---|
| 极限 | 导数定义 | “h 无限接近 0 时差商逼近的数”；导数是瞬时变化率 |
| 求和记号 Σ | 最小二乘、KKT、效用函数 | 线性性质、\(\sum(x_i-\bar x)=0\)、\(\sum(x_i-\bar x)^2=\sum x_i^2-n\bar x^2\) |
| 均值 / 方差 | 统计、SGLD 先验 | \(\bar x=\frac1n\sum x_i\)；\(\sigma^2=\frac1n\sum(x_i-\bar x)^2\) |
| 指数 / 对数 | 似然、贝叶斯、SGLD | \(\exp(a+b)=\exp a\exp b\)；\(\log(ab)=\log a+\log b\)；\(\log\exp x=x\) |
| 正态分布密度 | 先验 p₀ | \(\mathcal N(x;\xi,\sigma^2)=\frac1{\sqrt{2\pi\sigma^2}}\exp(-\frac{(x-\xi)^2}{2\sigma^2})\)；归一化常数保证总面积 1 |
| 独立性 | 先验=乘积 | 独立 ⟺ \(P(A\cap B)=P(A)P(B)\)；联合先验是各条目密度乘积 |
| 条件概率 | 贝叶斯 | \(P(A\mid B)=P(A\cap B)/P(B)\) |
| 全概率公式 | 贝叶斯分母 | 按互斥原因分解概率 |
| 线性方程组求解 | 最小二乘正规方程 | 代入法 / 消元法解两个未知数两个方程 |
| 矩阵（乘法/转置/单位阵/对角阵） | 全书 | 行×列；\((AB)^\top=B^\top A^\top\)；I 相当于 1 |
| 逆矩阵与可逆性 | 隐函数求导 (12) | \(AA^{-1}=I\)；无零特征值才可逆；\(\lambda I+\)半正定 必可逆 |
| 半正定矩阵 | Σ_U、Σ_V | \(x^\top Ax\ge0\)；\(vv^\top\) 半正定因为 \((x^\top v)^2\ge0\)；半正定之和仍半正定 |
| 迹 | Frobenius 范数 | tr(A)=对角线之和；tr(AB)=tr(BA) |
| 偏导数 / 梯度 | 全书 | 对其他变量求导时其余当常数；梯度=偏导向量；上升最快方向 |
| 链式法则（一元/多元） | 公式 (11) | 复合求导；多元沿所有路径累加 |
| 凸集 / 凸函数 | 公式 (1)(2)(3) | 弦在图像上方；局部最小=全局最小；f′=0 对凸函数足够 |
| 投影 / clamp | 公式 (10) | 可行域内最近点；方盒投影=逐坐标截断 |
| 范数（ℓ2/ℓ1/ℓ∞/Frobenius/核） | 全书 | 长度/大小度量；单位球形状；核范数=奇异值之和 |
| 次微分 | 公式 (14) | 不可导点处导数可取的范围；|x| 在 0 处为 [−1,1] |
| 贝叶斯公式 | 公式 (16) | 后验 ∝ 先验 × 似然（由条件概率定义推出） |
| 采样 / MCMC | SGLD | 从分布抽点；带噪声梯度上升可近似采样后验 |
| 秩 | 公式 (1) | 线性无关行（列）的最大个数；rank(X)≤k 表示 X 可由 k 个方向张成 |
| 观测算子 R_Ω | 公式 (1)–(5) | [R_Ω(A)]_ij = A_ij 若 (i,j)∈Ω 否则 0；只惩罚已观测位置 |
| 奇异值分解 SVD | 公式 (3)(14)、§4.2 | X=UΣVᵀ，U、V 列正交，Σ 非负对角；σ_i 为奇异值 |
| 核范数 | 公式 (3)(5) | ‖X‖_* = Σσ_i(X)，秩函数的凸包络；次微分公式的载体 |
| KKT 条件 | §4.1、公式 (14) | 约束最优化最优解的一阶必要条件；凸时充分；核范数用次微分包含关系写 |
| 隐函数求导 | 公式 (12) | F(Θ,M̃)=0 隐式定义 Θ(M̃)；∂Θ/∂M̃ = −(∂F/∂Θ)⁻¹(∂F/∂M̃) |
| Gram 矩阵 | 公式 (12) | Σ_U=Σ v_j v_jᵀ；半正定，保证 λI+Σ 可逆 |
| 谱范数 | 次微分公式 | ‖W‖₂ = 最大奇异值；核范数次微分中 ‖W‖₂ ≤ 1 |

## 输出时自检

生成推导页后，在页面底部或“覆盖自查”提示中列出：每个公式记号都能在本页找到解释，
没有“默认你会”的跳步。
