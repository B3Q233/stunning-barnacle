# 不确定性规则

## 标记

- `⚠️ 未从源码确认`：文档提到但未在当前代码中找到对应实现。
- `⚠️ 需要用户补充`：缺少仓库版本、数据、权重、硬件或运行结果。
- `推断：`：基于明确证据做出的非直接结论。

## 写法

先写可确认事实，再写缺口和影响。例如：“源码读取 `data_root`；⚠️ 未从源码确认该目录是否包含预处理索引。”

## 禁止

不得把典型 PyTorch 做法、其他版本 README、论文实验设置或个人经验伪装成当前仓库事实。不得用“应该”“通常”替代验证状态。