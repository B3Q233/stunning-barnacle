# IMPLEMENTATION_DOCS.md 模板

> 复制以下结构，逐步骤填写。每步完成验证后才填入对应章节，不要提前占位编造。

## 步骤① 数据处理

**用了什么方法**：{方法层面说明，如"用留一法划分训练/测试集：每个用户最新一条交互作为 test，倒数第二条作为 val"}

**原始数据格式**：
- 文件路径：{}
- 分隔符/格式：{}
- 字段含义：{}

**处理方法表**：
| 步骤 | 方法 | 参数 | 输入示例 | 输出示例 |
|---|---|---|---|---|
| 1 | {} | {} | {} | {} |

**输出规格**：训练/验证/测试集各自规模、格式：{}

**关键决策记录**：{标注是否为 AI 推断补全，及推断依据}

**验证结果**：{抽样对比结果，是否通过}

---

## 步骤② 数据导入

**实现类清单**：
| 类名 | 父类 | 职责 |
|---|---|---|
| PaperDataset | Dataset | {} |
| PaperDataLoader | DatasetProtocol | {} |

**batch 格式表**：
| split | 字段 | shape | dtype | 含义 |
|---|---|---|---|---|
| train | {} | {} | {} | {} |

**DataLoader 参数**：batch_size={}、shuffle={}、num_workers={}，依据：{}

**验证结果**：{shape/dtype 断言是否通过}

---

## 步骤③ 模型结构

**逐层结构表**：
| 层 | 类型 | 输入shape | 输出shape | 参数量 | 初始化方式 |
|---|---|---|---|---|---|
| 1 | {} | {} | {} | {} | {} |

**架构示意图**（文本缩进描述数据流）：
```
输入 (B, ...)
  → 层1 → (B, ...)
  → 层2 → (B, ...)
  → 输出 (B, ...)
```

**关键维度对照表**：{不同超参数下各层维度变化}

**验证结果**：{forward 跑通，输出 shape 是否一致}

---

## 步骤④ 模型评估

**评估协议表**：评估方式、负采样策略、过滤规则、排名范围：{}

**指标公式**：
$$
{LaTeX}
$$
对应代码逻辑：{}

**手工验证用例**：输入 {} → 预期输出 {} → 实际输出 {}

**验证结果**：{是否一致}

---

## 步骤⑤ 模型训练

**损失函数公式**：
$$
{LaTeX}
$$
各项权重系数及来源：{}

**优化器配置表**：
| 模型/阶段 | 优化器 | lr | 其他参数 | 来源 |
|---|---|---|---|---|
| {} | {} | {} | {} | {} |

**特殊训练策略**：{预训练+微调/学习率调度/warmup 等}

**1-batch 验证结果**：loss={}，参数更新确认={}，合理性说明={}

---

## 步骤⑥ 结果展示

**输出物清单**：{training_curve.png / comparison_table.md 等}

**论文报告值汇总**：
| 指标 | 论文报告值 | 来源（Figure/Table编号） |
|---|---|---|
| {} | {} | {} |

**对比结果**：
| 指标 | 论文值 | 复现值 | 相对差距 | 判定 |
|---|---|---|---|---|
| {} | {} | {} | {} | {完全对齐/部分对齐/未对齐} |

**缺口分析**：{如有差距，分析可能原因}
